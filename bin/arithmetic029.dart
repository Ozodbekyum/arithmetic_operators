// Create function func with arguments
// the task condition is in the Readme file
import 'dart:math';

num func(int a, int b) {
  return (a / 2) + (b / 3) + 2 * a * b;
}

void main() {
  print(func(2, 3));
}
